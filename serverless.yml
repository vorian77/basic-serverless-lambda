service: kssc-basic-serverless-lambda

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: us-east-2

functions:
  hello:
    handler: src/handler.generateRamdonNumber
    timeout: 60 # seconds
    memorySize: 128 # mb
